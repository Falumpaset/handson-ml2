/**
 *
 */
package de.immomio.broker;

import de.immomio.data.landlord.entity.prioset.Prioset;
import de.immomio.data.landlord.entity.property.Property;
import de.immomio.data.propertysearcher.bean.user.profile.PropertySearcherUserProfileData;
import org.junit.Before;

/**
 * @author Johannes Hiemer.
 */
public class CalculatorTest {

    private Prioset preferencePrioset;

    private PropertySearcherUserProfileData userProfile;

    private Property flat;

    @Before
    // TODO init enties
    public void before() {

        //    userProfile = UserprofileDataGenerator.createUserProfile();
        //    preferencePrioset = PreferencePriosetDataGenerator.createPreferencePrioset();
        //    flat = FlatDataGenerator.createFlat();
    }

    // TODO test calculation
    public void test() {
        //   double result = new Calculator().calculate(pr//eferencePrioset, userProfile, flat, 10);

        //   assertNotNull(result);
        //   assertTrue(result > 0);
    }

}
